import { StyleSheet } from "react-native";

export const footer=StyleSheet.create({
    footer: {
        flex:0.05,
        height: 60,
        backgroundColor: "#ddd",
        alignItems: "center",
        borderTopWidth: 1,
        borderColor: "#aaa",
        flexDirection:"row",
    },
    container:{
        flex:1,
        height:"100%",
        alignItems:"center",
        borderColor:"black",
        borderWidth:1,
    }
});